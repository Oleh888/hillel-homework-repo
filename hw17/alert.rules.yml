groups:
  - name: cpu
    rules:
      - alert: HighCpuUsage
        expr: sum(rate(container_cpu_usage_seconds_total[1m])) by (name) / sum(machine_cpu_cores) * 100 > 87
        for: 1m
        labels:
          severity: critical
